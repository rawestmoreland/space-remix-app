name: Fly Deploy
on:
  push:
    branches:
      - main

jobs:
  build-remix-image:
    name: Build remix image
    runs-on: ubuntu-latest
    concurrency: build-group
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/remix-app/Dockerfile
          push: true
          tags: registry.fly.io/remix-app:amd
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-remix:
    name: Deploy remix app
    needs: build-remix-image
    runs-on: ubuntu-latest
    concurrency: deploy-group
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy --remote-only --image registry.fly.io/remix-app:amd
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  build-backend-image:
    name: Build backend image
    runs-on: ubuntu-latest
    concurrency: build-group
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: packages/express-backend/Dockerfile
          push: true
          tags: registry.fly.io/launch-list-app-express-backend:amd
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-backend:
    name: Deploy backend
    needs: build-backend-image
    runs-on: ubuntu-latest
    concurrency: deploy-group
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy --remote-only --image registry.fly.io/launch-list-app-express-backend:amd
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

  deploy-trigger-dot-dev:
    name: Deploy trigger.dev
    runs-on: ubuntu-latest
    needs: [deploy-backend, build-backend-image]
    concurrency: deploy-group
    defaults:
      run:
        working-directory: packages/express-backend
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies
        run: npm install

      - name: 🚀 Deploy Trigger.dev
        env:
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN }}
        run: |
          npx trigger.dev@latest deploy
